//"use strict";

//var express = require('express');
//var port = 3000;
//var log = function (message) {
//    console.log('[' + date + '] ' + message);
//}
//
//module.exports = app.listen(app.get('port'), function () {
//    log('Server started listening on the port ' + port);
//});
